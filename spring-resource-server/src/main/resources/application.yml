server:
  port: 8083

spring:
  application:
        name: spring-resource-server

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000

logging:
  level:
    org:
      springframework:
        security: DEBUG
    com:
      ss:
        rs: DEBUG
